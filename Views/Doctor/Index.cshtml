@model IEnumerable<HMSApp.Models.Doctor>


@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>

@if (!Model.Any())
{
    <div class="alert alert-info">
        No doctors found.
    </div>
}
else
{
    <table class="table">
        <thead>
            <tr>
                <th>@Html.DisplayNameFor(model => model.First().Name)</th>
                <th>@Html.DisplayNameFor(model => model.First().Specialization)</th>
                <th>@Html.DisplayNameFor(model => model.First().ContactNumber)</th>
                <th>@Html.DisplayNameFor(model => model.First().AvailabilitySchedule)</th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>@Html.DisplayFor(modelItem => item.Name)</td>
                    <td>@Html.DisplayFor(modelItem => item.Specialization)</td>
                    <td>@Html.DisplayFor(modelItem => item.ContactNumber)</td>
                    <td>@Html.DisplayFor(modelItem => item.AvailabilitySchedule)</td>
                    <td>
                        <a asp-action="Edit" asp-route-id="@item.DoctorId">Edit</a> |
                        <a asp-action="Details" asp-route-id="@item.DoctorId">Details</a> |
                        <a asp-action="Delete" asp-route-id="@item.DoctorId">Delete</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
}
=======
@{
    ViewData["Title"] = "Doctor List";
}
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" />

<div class="container mt-4">
    <h2 class="mb-4">Doctor List</h2>
    <a class="btn btn-primary mb-3" asp-action="Create" asp-controller="Doctor">Add New Doctor</a>
    <table class="table table-bordered table-striped table-hover">
        <thead class="table-dark">
            <tr>
                <th>Name</th>
                <th>Specialization</th>
                <th>Contact</th>
                <th>Availability Schedule</th>
                <th>Actions</th>
            </tr>
        </thead>
        @if (Model != null && Model.Any())
        {
            <tbody>
                @foreach (var doctor in Model)
                {
                    <tr>
                        <td>@doctor.Name</td>
                        <td>@doctor.Specialization</td>
                        <td>@doctor.ContactNumber</td>
                        <td>@doctor.AvailabilitySchedule</td>
                        <td>
                            <a class="btn btn-info btn-sm" href="/Doctor/Details/@doctor.DoctorId">Details</a>
                            <a class="btn btn-warning btn-sm" href="/Doctor/Edit/@doctor.DoctorId">Edit</a>
                            <a class="btn btn-danger btn-sm" href="/Doctor/Delete/@doctor.DoctorId">Delete</a>
                        </td>
                    </tr>
                }
            </tbody>
        }
        else
        {
            <tbody>
                <tr>
                    <td colspan="5">No doctors found.</td>
                </tr>
            </tbody>
        }
    </table>
</div>
